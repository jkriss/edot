%h2 This month's category leaders

- @categories.each do |c|
  - if c.top_user.present?
    %p= "#{c.name.strip}: #{link_to_user(c.top_user)} with #{pluralize(c.top_user_score, 'dot')}"

%br/

%h2 Top users

%ol#top_users
  - @top_users.each do |u|
    %li
      = "#{link_to u.login, u} (#{u.points} dots)"
      - if logged_in? && current_user.is_admin?
        %span.contact
          - if u.twitter_id
            = link_to 'twitter', "http://twitter.com/#{u.login}"
          - if u.email
            = link_to 'email', "mailto:#{u.email}"