%h3 Recent Things

%ul.plain.things
  - @latest_things.each do |thing|
    %li.thing
      %span.user_link= link_to thing.user.login, thing.user
      &mdash;
      %span.category= "#{thing.subcategory.category.name}: #{thing.subcategory.name},"
      %span.points= "#{thing.subcategory.points} pts."
      %span.text= thing.text
      %span.link= link_to '&#x2192;', thing  

%h3 Top Do Gooders

%ul.plain.users
  - @top_users.each do |u|
    %li= "#{link_to(u.login, u)}: #{u.points} pts"
 